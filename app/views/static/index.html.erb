
<html>
<head>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">The Gossip Project</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
    <a class="nav-item nav-link active" href="<%=root_path%>">Accueil <span class="sr-only">(current)</span></a>
      <a class="nav-item nav-link active" href="<%=team_path %>">Team <span class="sr-only">(current)</span></a>
<a class="nav-item nav-link active" href="<%=contact_path %>">Contact <span class="sr-only">(current)</span></a>     
    </div>
  </div>
</nav>
</head>

<body>

<div class="container py-3 text-center">
<h1>Bienvenue <%= @user %>  !</h1>
<h3>Voici les potins :</h3>
</div>


<div class="row justify-content-center">
  <div class="col-lg-4 col-md-6 mb-3">
    <div class="card">
    <% @gossip.each do |gossip| %>
      <div class="card-body"> 
        <h5 class="card-title"><%= gossip.user.first_name %></h5>
        <p class="card-text">titre : <%= gossip.title %></p>
        <a href="/show/<%=gossip.id%>" class="btn btn-primary">Voir le potin</a></a>
      </div>
      <% end %>
    </div>
    
  </div>

</body>




</html>
